{
  "name": "met-font",
  "version": "1.0.0",
  "description": "WMO weather symbols as an emoji-style font",
  "homepage": "https://github.com/MetFont/metfont",
  "license": "CC-BY-3.0",
  "scripts": {
    "prepare-sources": "python3 helpers/assign-codepoints.py && python3 helpers/clean-sources.py",
    "prepare-fonts": "python3 helpers/combine-svg-paths.py --input-dir src/ && python3 helpers/normalize-svg.py --input-dir src/ --mapping data/codepoint-map.json --output-dir color/svg/",
    "generate-fonts": "./helpers/generate-fonts.sh",
    "generate-metadata": "python3 helpers/extract-glyphs.py metadata > font/metadata.json",
    "package-fonts": "cd font && zip -r MetFont.zip . -x '*.DS_Store'"
  }
}
